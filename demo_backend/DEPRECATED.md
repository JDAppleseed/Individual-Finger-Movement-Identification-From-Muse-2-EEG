# demo_backend is deprecated

The Vite demo backend and UI flow are deprecated. The production live workflow
now runs entirely through the PySide6/PyQt desktop app (`eeglab_wrapper_ui.py`).

This folder is retained **only** as a reference during validation and will be
removed once the desktop live flow is fully validated.
